spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos-registry:8848
        username: nacos
        password: nacos
      config:
        server-addr: http://nacos-registry:8848
        file-extension: yaml
        username: nacos
        password: nacos
logging:
  level:
    root: info